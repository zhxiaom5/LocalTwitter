# AGENTS.md

本文件是项目协作与开发宪法。所有人类开发者与 AI Agent 在本仓库内工作时，必须遵守本文件。本文只定义规则、边界、流程和质量标准，不包含任何业务逻辑。

## 核心原则

- 简单优先：在满足需求和质量的前提下，选择最少组件、最少抽象、最少运行依赖的方案。
- 测试先行：任何功能开发前，必须先完成对应测试 case 设计；未设计测试 case，不得进入实现阶段。
- 小步交付：任务必须拆分为可验证的小任务，每个任务都有明确验收方式。
- 可维护优先：代码结构、命名、错误处理、测试和文档必须服务于长期维护。
- 禁止隐式复杂化：不得为了假想扩展提前引入缓存、消息队列、微服务、复杂中间件或过度封装。

## 技术边界

### 服务端

- 服务端使用 Go。
- 数据存储使用 SQLite。
- 不引入独立缓存组件。
- 不引入消息队列、服务网格、分布式任务系统等额外中间件。
- 优先使用 Go 标准库；第三方依赖必须有明确必要性，并在任务设计中说明理由。
- 服务端必须保持清晰分层，至少区分入口层、应用逻辑层、数据访问层和基础设施配置。
- SQLite 访问必须通过统一的数据访问边界进行，禁止业务代码随意拼接和散落 SQL。
- 所有数据库 schema 变更必须可追踪、可重复执行、可测试。
- 错误处理必须显式，禁止吞掉错误。
- 日志必须服务于排障，禁止记录敏感信息。

### Web 前端

- 本项目为单体部署模式，不采用前后端分离；Web 前端必须随同 Go 服务端一体构建、发布和运行。
- 前端实现可以使用服务端模板、静态资源增强或嵌入式前端资源方案，但最终交付物必须由同一个 Go 程序统一承载。
- 前端目录必须按页面、组件、状态、接口访问、测试和通用工具清晰组织。
- UI 状态必须覆盖加载、空状态、错误、成功和权限受限等通用场景。
- 页面交互、表单、路由、状态和副作用必须有清晰边界。
- 前端不得依赖未定义的服务端行为；页面契约、接口契约和资源加载方式必须通过测试验证。

## 架构规则

- 项目采用单 Go 服务架构，由同一个 Go 程序同时承载 Web 前端和服务端能力。
- 服务端负责数据一致性、权限校验、输入校验和持久化。
- 前端负责用户交互、表现层状态和接口契约调用。
- 前后端边界必须体现在清晰的模块分层中，禁止因为同进程部署而混淆页面渲染、应用逻辑、数据访问和基础设施职责。
- 共享契约必须以可测试形式存在，例如接口定义、模板输入输出约定、类型定义、OpenAPI 文档或等价机制。
- 跨层调用必须单向、明确，禁止循环依赖。
- 配置必须通过环境变量、配置文件或启动参数管理，禁止硬编码环境差异。
- 本地开发、测试和生产配置必须可区分。

## 开发流程

每个功能必须按以下顺序推进：

1. 明确需求边界、非目标和验收标准。
2. 拆分任务列表。
3. 设计测试 case，覆盖单元测试、联调测试、集成测试和真实用户全流程访问测试。
4. 评审测试 case 是否覆盖成功路径、失败路径、边界条件和回归风险。
5. 编写或更新测试。
6. 实现功能。
7. 运行全部相关测试。
8. 修复问题，直到所有测试 100% 通过。
9. 更新必要的开发文档或接口契约。

禁止在第 3 步完成前进行功能实现。

## 测试规范

项目必须具备完整测试框架，并持续维护以下测试层级：

- 单元测试：验证函数、模块、组件和纯逻辑行为。
- 联调测试：验证页面与服务端接口契约、请求响应格式、错误码和边界输入。
- 集成测试：验证服务端、数据库、配置、迁移和关键模块组合后的行为。
- 真实用户全流程访问测试：通过浏览器自动化模拟用户从入口到完成目标路径的完整操作。

测试 case 设计必须包含：

- 测试名称。
- 前置条件。
- 操作步骤。
- 期望结果。
- 覆盖的风险点。
- 所属测试层级。

测试要求：

- 新功能必须新增或更新测试。
- Bug 修复必须先补充能复现问题的回归测试。
- 数据库相关逻辑必须覆盖迁移、读写、事务、异常输入和数据约束。
- 前端关键交互必须覆盖用户可见行为，而不是只测试实现细节。
- 单 Go 服务场景下，必须覆盖页面渲染、静态资源提供、表单提交和接口响应之间的协同行为。
- 测试必须可重复运行，不依赖执行顺序。
- 测试数据必须隔离，禁止污染开发或生产数据。
- 真实用户全流程访问测试必须在功能完成前通过。

## 质量门禁

功能完成必须同时满足以下条件：

- 原始需求已完整实现。
- 任务列表全部完成。
- 单元测试 100% 通过。
- 联调测试 100% 通过。
- 集成测试 100% 通过。
- 真实用户全流程访问测试 100% 通过。
- 代码格式化、静态检查和类型检查全部通过。
- 无已知阻塞缺陷。
- 无未说明的临时方案。
- 必要文档、接口契约和迁移说明已更新。

任何一项不满足，功能不得标记为完成。

## 代码规范

- Go 代码必须通过 `gofmt` 或等价格式化。
- Go 测试必须使用标准测试能力或项目统一测试框架。
- 前端代码必须通过统一格式化、Lint 和类型检查。
- 命名必须表达意图，避免无意义缩写。
- 函数、组件和模块应保持单一职责。
- 注释只解释必要背景、约束和非显然决策，禁止重复代码表面含义。
- 公共接口变更必须同步更新契约和测试。
- 禁止提交无关格式化、无关重构或无关依赖变更。

## 依赖规则

- 新增依赖必须证明必要性。
- 能用标准库或已有依赖完成的，不新增依赖。
- 禁止引入与项目技术边界冲突的缓存组件、中间件或大型框架。
- 依赖必须稳定、维护活跃、许可证可接受。
- 删除未使用依赖必须同步更新构建和文档。

## 数据库规则

- SQLite 是唯一默认持久化数据库。
- schema 变更必须有迁移路径。
- 迁移必须可在空库和已有库上可靠执行。
- 测试必须使用独立测试数据库或临时数据库。
- 数据访问层必须覆盖 SQL 错误、约束冲突和事务回滚。
- 禁止在测试中依赖生产或个人本地数据库。

## 安全与隐私

- 输入必须在服务端进行校验。
- 权限、认证和敏感操作必须由服务端强制执行。
- 禁止将密钥、令牌、密码或个人敏感数据提交到仓库。
- 日志、错误信息和测试快照不得泄露敏感信息。
- 任何外部输入都必须按不可信数据处理。

## Agent 工作规则

- 修改代码前必须先阅读相关文件和现有模式。
- 开发前必须先列出测试 case 或确认已有测试 case 覆盖需求。
- 不得绕过测试门禁声称任务完成。
- 遇到失败测试，必须优先定位并修复根因。
- 不得回滚用户未授权的改动。
- 不得引入与本文件冲突的技术组件。
- 最终回复必须说明完成内容、测试结果和任何未完成风险。

## 禁止事项

- 禁止在没有测试 case 设计的情况下开始功能开发。
- 禁止跳过失败测试并标记完成。
- 禁止引入 Redis、Memcached、Kafka、RabbitMQ、Elasticsearch 等额外缓存或中间件组件，除非本文件被正式更新。
- 禁止把业务逻辑写入本文件。
- 禁止把临时脚本、调试代码或个人环境配置作为正式实现提交。
- 禁止依赖手工操作作为唯一验收方式。
